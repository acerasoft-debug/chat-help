
<div style="max-width:860px;margin:0 auto;padding:52px 32px">
  <h1 style="font-family:'Playfair Display',serif;font-size:36px;font-weight:700;color:var(--td,#1C1C1C);margin-bottom:24px">{{ page.title }}</h1>
  <div style="font-size:15px;color:var(--tm,#4A4A4A);line-height:1.8">{{ page.content }}</div>
</div>
