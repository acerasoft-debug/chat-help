# PostgreSQL connection (local dev)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/psp_dev"

# Stripe — payments (manual capture = escrow-style hold) + Connect payouts
STRIPE_SECRET_KEY=""
STRIPE_PUBLISHABLE_KEY=""
STRIPE_WEBHOOK_SECRET=""

# Session signing key — min 32 chars, generate with: openssl rand -hex 32
SESSION_SECRET=""

# Public base URL (used in magic links)
APP_URL="http://localhost:3000"

# Identity verification provider: "mock" (dev) | "stripe_identity" | "sumsub"
KYC_PROVIDER="mock"
IDENTITY_PROVIDER_API_KEY=""
